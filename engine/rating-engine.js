'use strict';
function assignSelectiveRatings(stocks){
 const e=stocks.filter(s=>s.qualifiesForBuyList&&s.dataIntegrity?.isUsable!==false&&Number.isFinite(s.expectedReturn)&&s.expectedReturn>0).sort((a,b)=>(b.portfolioManagerScore-a.portfolioManagerScore)||(b.businessQualityScore-a.businessQualityScore)),n=stocks.length,ex=Math.max(1,Math.floor(n*.01)),sb=Math.max(3,Math.floor(n*.05)),buy=Math.max(10,Math.floor(n*.20));
 e.forEach((s,i)=>{const rq=s.returnQualityScore??s.returnEngineV2?.returnQualityScore??70,md=s.returnEngineV2?.multipleDominated===true;const hx=s.businessQualityScore>=82&&s.compounderScore>=78&&s.confidenceScore>=72&&s.downsideProtectionScore>=55&&s.expectedReturn>=.15&&rq>=65&&!md,hs=s.businessQualityScore>=72&&s.compounderScore>=68&&s.confidenceScore>=62&&s.downsideProtectionScore>=42&&s.expectedReturn>=.12&&rq>=52;s.rating=i<ex&&hx?'Exceptional':i<sb&&hs?'Strong Buy':i<buy&&s.businessQualityScore>=62&&s.expectedReturn>=.10?'Buy':'Watch';});
 const set=new Set(e);for(const s of stocks)if(!set.has(s)){const neg=(s.expectedReturn??-1)<0,over=s.marginOfSafety!=null&&s.marginOfSafety<-.20,high=(s.downsideRiskScore??50)>=72;s.rating=neg||over||high?'Sell':'Watch';}const t={Exceptional:6,'Strong Buy':5,Buy:4,Hold:3,Watch:2,Sell:1};return [...stocks].sort((a,b)=>(t[b.rating]-t[a.rating])||(b.portfolioManagerScore-a.portfolioManagerScore)||(b.businessQualityScore-a.businessQualityScore));
}
module.exports={assignSelectiveRatings};
